@Book{ {{ document.pid  }},
 {% if document.creator is defined %}}author    = "{{ document.creator|join(' and ') }}",{% endif %}
 title     = "{{ document.title|first }}",
 publisher = "{{ document.publisher|join(' and ') }}",
 year      =  {{ document.year_publish }},
 address   = "{{ document.place_publish|join(' and ') }}"
}
